# نقشهٔ راه نسخه‌ها

| نسخه | محتوا |
|---|---|
| 1.0.0 (فعلی) | تمام فازها: MVP آپلود/تحویل، پنل ادمین ربات، عضویت اجباری، آپلود گروهی/آلبوم، کیف پول/پلن‌ها، کارت‌به‌کارت + CentralPay + زرین‌پال + زیبال + استارز، پنل وب کامل، بازبینی آپلود کاربران، پوشه‌ها، جستجو، تبلیغات، آمار+CSV، پشتیبان/بازیابی، گزارش تخلف، متن‌های قابل‌ویرایش، REST API v1، لایسنس + سرور فعال‌سازی |
| 1.1.x (پیشنهادی) | اعلان‌های زمان‌بندی‌شده، برچسب‌ها، آمار درگاه‌به‌تفکیک روز |
| 1.2.x (پیشنهادی) | چند-ربات از یک پنل، نقش‌های پنل (viewer/operator) |

شماره‌گذاری معنایی (`VERSION`)؛ هر انتشار از مسیر `update.sh` (پشتیبان خودکار +
health check + changelog).
