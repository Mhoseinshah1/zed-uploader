# ZedUploader — Phase 1 dependencies (pinned)
aiogram==3.13.1
fastapi==0.115.2
uvicorn[standard]==0.31.1
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
alembic==1.13.3
redis==5.1.1
pydantic==2.9.2
pydantic-settings==2.5.2
structlog==24.4.0
httpx==0.27.2
python-multipart==0.0.12
jinja2==3.1.4
bcrypt==4.2.0
pytest==8.3.3
pytest-asyncio==0.24.0
# aiosqlite + fakeredis are used only by the offline test suite (in-memory
# SQLite + fake Redis); no live DB/Redis required to run the unit tests.
# Money-safety tests are integration tests gated on TEST_DATABASE_URL (real PG).
aiosqlite==0.20.0
fakeredis==2.25.1
